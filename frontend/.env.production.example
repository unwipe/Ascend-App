# Ascend Frontend - Production Environment Variables
# These variables will be set in Vercel dashboard
# DO NOT commit actual .env.production file to Git!

# ============================================================================
# Google OAuth (Production Client)
# ============================================================================
# Use the SAME Client ID as backend
# Get from Google Cloud Console → Credentials
# IMPORTANT: Must start with REACT_APP_ for Create React App!
REACT_APP_GOOGLE_CLIENT_ID=your_production_google_client_id.apps.googleusercontent.com

# ============================================================================
# Backend API URL
# ============================================================================
# Your production backend URL (Render URL)
# Format: https://your-backend-name.onrender.com
# IMPORTANT: No trailing slash! Must start with REACT_APP_!
REACT_APP_BACKEND_URL=https://ascend-backend-xxxx.onrender.com

# ============================================================================
# Build-time Configuration (Vercel handles these automatically)
# ============================================================================
# These are set automatically by Vercel during build:
# NODE_ENV=production
# CI=true
