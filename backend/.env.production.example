# Ascend Backend - Production Environment Variables
# Copy this file and fill in your actual values
# DO NOT commit the actual .env.production file to Git!

# ============================================================================
# MongoDB Atlas (Production)
# ============================================================================
# Get this from MongoDB Atlas → Clusters → Connect → Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGO_URL=mongodb+srv://ascend_admin:YOUR_PASSWORD_HERE@your-cluster.mongodb.net/?retryWrites=true&w=majority

# Database name (use separate database for production)
DB_NAME=ascend_prod

# ============================================================================
# JWT Configuration
# ============================================================================
# Generate a strong random secret (64+ characters)
# Command: openssl rand -hex 32
# Or: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your_64_character_random_string_here

# JWT algorithm (keep as HS256)
JWT_ALGORITHM=HS256

# JWT expiration in minutes (43200 = 30 days)
JWT_EXPIRATION_MINUTES=43200

# ============================================================================
# Google OAuth (Production Client)
# ============================================================================
# Get this from Google Cloud Console → Credentials
# Create a SEPARATE OAuth client for production (not the dev one!)
# Format: 123456789-abcdefg.apps.googleusercontent.com
GOOGLE_CLIENT_ID=your_production_google_client_id.apps.googleusercontent.com

# ============================================================================
# CORS Configuration
# ============================================================================
# Your production frontend URL (Vercel URL)
# Format: https://your-app-name.vercel.app
# IMPORTANT: No trailing slash!
CORS_ORIGINS=https://your-app-name.vercel.app

# ============================================================================
# Environment Identifier
# ============================================================================
# Set to "production" for production, "staging" for staging, etc.
ENVIRONMENT=production

# ============================================================================
# Optional: Python Version (for Render)
# ============================================================================
PYTHON_VERSION=3.11.0
